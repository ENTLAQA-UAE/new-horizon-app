# =====================================================
# Kawadir ATS - Environment Variables
# =====================================================
# Copy this file to .env.local and fill in the values.
# Required variables are marked with (REQUIRED).
# =====================================================

# ----- Supabase (REQUIRED) -----
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----- Platform Configuration (REQUIRED) -----
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000
PLATFORM_NAME=Kawadir ATS
PLATFORM_LOGO_URL=
PLATFORM_PRIMARY_COLOR=#4F46E5
PLATFORM_SECONDARY_COLOR=#7C3AED

# ----- Encryption (REQUIRED) -----
# Used to encrypt sensitive credentials (integration tokens, API keys) at rest.
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-encryption-key-hex-64-chars
ENCRYPTION_SECRET=your-encryption-secret
ENCRYPTION_SALT=your-encryption-salt

# ----- AI / LLM -----
# At least one AI provider key is needed for AI features (resume parsing, job generation, candidate scoring).
ANTHROPIC_API_KEY=

# ----- Email (Default Provider) -----
# Used as the platform-level default email sender. Organizations can configure their own provider.
RESEND_API_KEY=
EMAIL_FROM=noreply@kawadir.io

# ----- Google Integration (Calendar & Meetings) -----
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/callback

# ----- Microsoft Integration (Outlook & Teams) -----
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:3000/api/microsoft/callback

# ----- Zoom Integration -----
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
ZOOM_REDIRECT_URI=http://localhost:3000/api/zoom/callback
ZOOM_WEBHOOK_SECRET_TOKEN=

# ----- Vercel (Deployment) -----
VERCEL_API_TOKEN=
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=

# ----- Sentry (Error Monitoring) -----
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
